#include <iostream>
#include <vector>
#include <set>
using namespace std;

int main(){
  set<int> s;
  int number;
  while(!cin.eof()){
    cin >> number;
    s.insert(number);
    
  }
    
    return 0;
}